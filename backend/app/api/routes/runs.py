"""Runs endpoint — re-exported from pipeline.py for clarity.

The /api/runs endpoint lives in pipeline.py since it shares
the same Redis dependencies and models. This module exists
only to match the architecture_v3 directory structure.
"""

# The /api/runs endpoint is defined in pipeline.py
# This file is intentionally minimal — no duplicate route definitions.
